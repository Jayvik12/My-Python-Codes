'''src_ip = input("Enter the source: ")
dest_ip = input("Enter the destination: ")
#trace = "tracert source {} destination {}".format(src_ip,dest_ip)
#trace = "tracert " "source" + " " + src_ip + " " + "destination" + " " + dest_ip 
trace = f"source {src_ip} destination {dest_ip}" #f string can only work with STR
print (trace)
'''
